<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.thomasclaxton.corkboard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name="net.thomasclaxton.corkboard.activities.CreateNoteActivity"
            android:theme="@style/DarkTheme.NoActionBar">
        </activity>
        <activity
            android:name="net.thomasclaxton.corkboard.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/DarkTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>